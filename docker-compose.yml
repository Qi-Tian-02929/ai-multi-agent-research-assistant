services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app/data:/app/app/data
    env_file:
      - .env
    depends_on:
      - redis
    restart: unless-stopped

  redis:
    image: redis:7
    ports:
      - "6379:6379"
    restart: unless-stopped
